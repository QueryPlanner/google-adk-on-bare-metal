# This file belongs only to the Collector container.
# Supply only the hostname and optional port; the config hardcodes HTTPS and
# the exact trace path.
OTEL_GATEWAY_LANGFUSE_AUTHORITY=cloud.langfuse.com
